<!DOCTYPE html> <html lang="en"> <head> <meta charset="UTF-8"> <meta name="viewport" content="width=device-width, initial-scale=1.0"> <style> /* 整体页面布局 */ body { margin: 0; padding: 0; display: flex; flex-direction: row; width: 100vw; height: 100vh; overflow: hidden; } /* 原始页面内容样式 */ .original-content { flex: 1; background-color: #f0f0f5; transition: all 0.5s ease; position: relative; } /* 缩略后的原始页面内容样式 */ .original-content.thumbnail { flex: 0.5; transform: scale(0.8); transform-origin: center right; } /* 左边弹出半个页面的样式 */ .left-panel { width: 0; background-color: #ccc; transition: all 0.5s ease; overflow: hidden; order: -1; /* 确保面板在原始内容左侧 */ position: relative; } /* 左边弹出半个页面展开后的样式 */ .left-panel.open { width: 50%; padding: 10px; } /* “+”号按钮样式（在原始内容区域） */ .open-btn { cursor: pointer; font-size: 24px; position: absolute; top: 10px; left: 10px; z-index: 10; } /* “-”号按钮样式（在左侧面板内） */ .close-btn { cursor: pointer; font-size: 24px; position: absolute; top: 10px; right: 10px; z-index: 10; display: none; } .left-panel.open .close-btn { display: block; } </style> <script> function togglePanel() { const leftPanel = document.querySelector('.left-panel'); const originalContent = document.querySelector('.original-content'); const openBtn = document.querySelector('.open-btn'); if (leftPanel.classList.contains('open')) { leftPanel.classList.remove('open'); originalContent.classList.remove('thumbnail'); openBtn.style.display = 'block'; } else { leftPanel.classList.add('open'); originalContent.classList.add('thumbnail'); openBtn.style.display = 'none'; } } </script> </head> <body> <!-- 左边弹出的半个页面 --> <div class="left-panel"> <span class="close-btn" onclick="togglePanel()">-</span> <p>这是左边弹出页面的内容。</p > <!-- 这里可以添加更多左边弹出页面的元素 --> </div> <!-- 原始页面内容 --> <div class="original-content"> <span class="open-btn" onclick="togglePanel()">+</span> <p>这是原始页面的内容。</p > <!-- 这里可以添加更多原始页面的元素 --> </div> </body> </html>